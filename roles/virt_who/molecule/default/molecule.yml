---
dependency:
  name: galaxy
driver:
  name: ${DRIVER_NAME:-podman}
platforms:
  - name: centos9
    image: quay.io/centos/centos:stream9
    command: /sbin/init
provisioner:
  name: ansible
verifier:
  name: ansible
